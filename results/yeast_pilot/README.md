# Superseded: pre-correction yeast pilot

These files were generated by the pipeline **before** a scoring defect was
corrected, and they are kept as a record of that run rather than regenerated
in place. Do not read them as current results.

The one candidate they contain, **P07264 (LEU1)**, scored 0.955 because the
composite score's depth term was fed fpocket's *mean local hydrophobic density*
(32.1 for this pocket) and treated it as a distance, awarding full credit above
15. The pocket's true burial depth is 2.84 Å. Re-scoring with that one input
corrected gives 0.711, below the 0.75 threshold, and re-screening the same 499
proteins under the corrected scorer yields no candidates.

- Why the score fell, with the component breakdown: `manuscript/RESULTS_DRAFT.md`
  ("The yeast pilot does not yet yield a candidate").
- The re-screen itself: `.github/workflows/yeast-rescreen.yml`, which writes to
  `results/yeast_rescreen/` so it cannot resume from the cache stored here.
